version: "3.8"

# Dev override: adds tailwind watcher and mounts source for hot-reload

services:
  web:
    volumes:
      - ..:/app:z

  tailwind:
    image: oven/bun:latest
    working_dir: /app
    command: bun run watch:css
    volumes:
      - ..:/app:z
